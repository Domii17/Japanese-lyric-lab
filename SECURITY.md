# 安全说明

## API Key

API Key 只能配置在服务器环境变量中。不要将密钥写入代码、提交到 GitHub，或使用带有 `NEXT_PUBLIC_` 前缀的变量名。

如果密钥意外出现在提交、截图或日志中，请立即在对应 AI 服务后台撤销并重新生成。仅从 Git 历史中删除密钥不能使旧密钥恢复安全。

## 报告漏洞

请优先使用 GitHub 仓库的 Private vulnerability reporting 或 Security Advisory 私下报告漏洞。不要在公开 Issue 中粘贴密钥、用户文本、服务端日志或可直接利用的攻击细节。

自行部署者应根据流量和风险增加持久化限流、调用额度、监控与告警。项目内置的内存限流只适合个人部署和低流量使用。
