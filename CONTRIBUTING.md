# 参与贡献

感谢你帮助改进歌词拆解。提交代码前，请先确认改动不包含完整的受版权保护歌词、真实 API Key 或用户数据。

## 开发流程

1. Fork 仓库并创建功能分支。
2. 运行 `pnpm install` 安装依赖。
3. 使用 `pnpm dev` 启动本地开发环境。
4. 提交前运行 `pnpm lint` 和 `pnpm build`。
5. 在 Pull Request 中说明问题、实现方式和验证结果。

## 内容与隐私

- 测试夹具和截图应使用原创、公共领域或获得授权的文本。
- 不要提交 `.env.local`、API Key、访问令牌或生产日志。
- 新增外部服务时，应同步更新 README、隐私说明和版权说明。
- 改动日语读音规则时，请提供能复现问题的最小文本和预期读音。

## 代码风格

- 优先沿用项目现有的 React、TypeScript 和 CSS 写法。
- 保持基础切分功能不依赖 AI。
- 面向初学者的显示文本应简洁、明确，并在移动端保持可读。
